I want to redesign this application fully.
By default, it should run on standard online mode - it should point to backend.
It should be standard sidebar AI-assistant with backend, with ability to choose model, etc.
And there is should be button for offline mode, which is switching to current app.

Give a implementation plan.ds